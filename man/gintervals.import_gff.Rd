% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals-import.R
\name{gintervals.import_gff}
\alias{gintervals.import_gff}
\title{Import intervals from a GFF/GTF file}
\usage{
gintervals.import_gff(file = NULL, feature = NULL, strand = TRUE, attrs = TRUE)
}
\arguments{
\item{file}{path to a GFF/GTF file.}

\item{feature}{optional feature-type filter (column 3 of GFF). Pass a
character vector to keep only those types (e.g. \code{"exon"},
\code{c("gene", "transcript")}).}

\item{strand}{if \code{TRUE}, include the \code{strand} column.}

\item{attrs}{if \code{TRUE}, include the raw attributes string as
column \code{attrs}. The attribute string is not parsed.}
}
\value{
A 1D intervals data frame with columns chrom, start, end, and
  optionally strand, type, source, score, attrs.
}
\description{
Reads a GFF3 or GTF file (optionally gzipped) and returns a misha 1D
intervals data frame. GFF/GTF are 1-based and inclusive on both ends;
coordinates are converted to 0-based half-open by subtracting 1 from
\code{start} and leaving \code{end} as-is.
}
\details{
Chromosome names are normalized through the active database's
\code{CHROM_ALIAS} mechanism.
}
\seealso{
\code{\link{gintervals.import_bed}},
  \code{\link{gintervals.import_vcf}}.
}
\keyword{~GFF}
\keyword{~GTF}
\keyword{~import}
\keyword{~intervals}
