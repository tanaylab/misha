% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals-import.R
\name{gintervals.import_bed}
\alias{gintervals.import_bed}
\title{Import intervals from a BED file}
\usage{
gintervals.import_bed(file = NULL, name = TRUE, score = TRUE, strand = TRUE)
}
\arguments{
\item{file}{path to a BED file (\code{.bed}, \code{.bed.gz}, or
\code{.bed.zip}).}

\item{name}{if \code{TRUE} and a 4th column exists, include it as
\code{name}.}

\item{score}{if \code{TRUE} and a 5th (numeric) column exists, include
it as \code{score}.}

\item{strand}{if \code{TRUE} and a 6th column exists, include it as
\code{strand} (mapped to \code{1}/\code{-1}/\code{0}).}
}
\value{
A 1D intervals data frame, sorted by chrom and start.
}
\description{
Reads a BED/BED.gz/BED.zip file and returns a misha 1D intervals data
frame. Track/browser/comment header lines are skipped automatically.
Chromosome names are normalized through the active database's
\code{CHROM_ALIAS} mechanism (so \code{chr1} <-> \code{1} works without
explicit configuration).
}
\details{
BED is already 0-based half-open, so coordinates are taken as-is.
}
\seealso{
\code{\link{gintervals.import_gff}},
  \code{\link{gintervals.import_vcf}}.
}
\keyword{~BED}
\keyword{~import}
\keyword{~intervals}
