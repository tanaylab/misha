% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals-to-mat.R
\name{[.intervs_mat}
\alias{[.intervs_mat}
\title{Subset an intervs_mat preserving interval identity}
\usage{
\method{[}{intervs_mat}(x, i, j, ..., drop = TRUE)
}
\arguments{
\item{x}{an \code{intervs_mat}}

\item{i}{row selector (logical, integer, or character matching rownames)}

\item{j}{column selector}

\item{...}{unused}

\item{drop}{if \code{TRUE} and a single row is selected via \code{i},
collapse to a named vector and drop the class. Defaults to \code{TRUE}
to match base matrix \code{[} for row selection.}
}
\value{
An \code{intervs_mat} (still 2D), or a numeric vector (degenerate).
}
\description{
Row subset of an \code{intervs_mat} subsets \code{attr(., "intervals")} in
parallel. Column-only subset leaves the intervals attribute unchanged.
Single-row results with \code{drop = TRUE} return a plain numeric vector
(class dropped).
}
