% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track-export.R
\name{gtrack.export_bigwig}
\alias{gtrack.export_bigwig}
\title{Export a track to BigWig format}
\usage{
gtrack.export_bigwig(track, file, intervals = NULL, iterator = NULL)
}
\arguments{
\item{track}{track name or track expression (character string)}

\item{file}{output file path (typically ending in \code{.bw} or
\code{.bigwig}).}

\item{intervals}{genomic intervals to export. If \code{NULL} (default),
the entire genome (\code{.misha$ALLGENOME}) is used.}

\item{iterator}{iterator bin size. If \code{NULL} (default), the iterator
is determined automatically from the track expression.}
}
\value{
\code{NULL} (invisible). Called for its side effect of writing a
  file.
}
\description{
Exports a track or track expression to BigWig format by first creating a
temporary bedGraph file and then converting it using \code{bedGraphToBigWig}
(or \code{wigToBigWig} as a fallback).
}
\details{
This function requires the UCSC \code{bedGraphToBigWig} utility to be
installed and available on the system PATH, or bundled with the misha
package. If not found, the function will raise an error with installation
instructions.
}
\examples{
\dontrun{
gdb.init_examples()

# Export to BigWig (requires bedGraphToBigWig)
gtrack.export_bigwig("dense_track", "/tmp/dense.bw")

# With specific region
gtrack.export_bigwig("dense_track", "/tmp/dense_chr1.bw",
    intervals = gintervals(1, 0, 1e6)
)
}

}
\seealso{
\code{\link{gextract}}, \code{\link{gtrack.export_bedgraph}},
  \code{\link{gtrack.info}}
}
\keyword{~bigwig}
\keyword{~export}
\keyword{~track}
